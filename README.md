# people
Even though the layout is presented in a horizontal desktop format, the structural theme is built with Flexbox/Grid columns. This guarantees that when the layout shrinks to mobile screens, the columns seamlessly stack vertically without breaking the core design.
